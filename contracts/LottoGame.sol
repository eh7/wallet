pragma solidity ^0.4.23;

import "./LottoFactory.sol";

contract LottoGame is LottoFactory {

/*
  event NewLottoGame(uint lottoId, string name, uint timestamp, uint count); 
    
  mapping (uint => address) public lottoGames;

  function playLotto(uint _lottoId, uint _number) public {
    uint timestamp = now;
    uint id = lottos.push(Lotto(_name, timestamp));
    lottoOwner[id] = msg.sender;
    lottoCount[msg.sender]++;
    emit NewLotto(id, _name, timestamp, lottoCount[msg.sender]); 
  }
*/

}

